name: weather_uts_app
description: "Weather App UTS"
publish_to: "none"

version: 1.0.0+1

# 🔥 WAJIB ADA
environment:
  sdk: "^3.9.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 DEPENDENCY UTAMA
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
